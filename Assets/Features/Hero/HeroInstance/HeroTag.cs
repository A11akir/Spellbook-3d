using UnityEngine;

namespace Features.Hero.HeroInstance
{
    public class HeroTag : MonoBehaviour
    {
        public Transform GetTransform()
        {
            return transform;
        }
    }
}